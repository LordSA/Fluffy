<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= bot.user.username %> - Dashboard</title>
    <style>
        body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; background-color: #23272a; color: #ffffff; padding: 20px; }
        .stats-grid { display: flex; justify-content: center; gap: 20px; margin-top: 50px; }
        .card { background: #2c2f33; padding: 20px; border-radius: 8px; width: 200px; text-align: center; box-shadow: 0 4px 6px rgba(0,0,0,0.3); }
        .card h3 { margin: 0; color: #99aab5; font-size: 1.2rem; }
        .card p { font-size: 2.5rem; margin: 10px 0 0; font-weight: bold; color: #7289da; }
        .back-btn { display: block; margin-top: 20px; text-align: center; color: #7289da; text-decoration: none; }
    </style>
</head>
<body>
    <h1 style="text-align: center;">Bot Dashboard</h1>
    
    <div class="stats-grid">
        <div class="card">
            <h3>Servers</h3>
            <p><%= guilds %></p>
        </div>
        <div class="card">
            <h3>Users</h3>
            <p><%= users %></p>
        </div>
        <div class="card">
            <h3>Music Engine</h3>
            <p><%= bot.config.Music.Engine.toUpperCase() %></p>
        </div>
    </div>

    <a href="/" class="back-btn">← Back to Home</a>
</body>
</html>